<!doctype html>
<html>
    <head>
        {{css}}
    </head>
    <body>
        <div class="container">
            {{header}}
            <h1>{title}</h1>
            {image|<img src="{}" />}
            {paragraphs%1|
                <p>{image|<img src="{}"{$alignRight=true| class="right"} />}{text}</p>
            }
            {{footer}}
        </div>
        {{js}}
    </body>
</html>