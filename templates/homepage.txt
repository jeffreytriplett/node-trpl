<!doctype html>
<html>
    <head>
        {{css}}
    </head>
    <body>
        <div class="container">
            {{header}}
            <p class="first">{page$overview}</p>
            {stories|
                <div class="row">
                    {feature|
                        <a class="half" href="{path}">
                            <img src="{image}" />
                            <h2>{title}</h2>
                            <p>{summary}</p>
                        </a>
                     }
                    {popular|
                        <div class="half">
                            {%1|
                                {%<4|
                                    <a class="row" href="{path}">
                                        <div class="third">
                                            <h3 class="first">{title}</h3>
                                            <p>{summary}</p>
                                        </div>
                                        <img src="{image}" class="sixth" />
                                    </a>
                                }
                            }
                        </div>
                    }
                </div>
                {recent%4|
                    <div class="row">
                        {%1|
                            {$%<-1;%<3|
                                <a class="fourth" href="{path}">
                                    <h4 class="first">{title}</h4>
                                    <p>{summary}</p>
                                </a>
                            }
                        }
                        {%=-1|
                            <div class="half">
                                <form action="/search/" class="row">
                                    <input type="text" name="q" class="third narrow-bottom" />
                                    <input type="submit" class="search sixth narrow-bottom" value="Search" />
                                </form>
                                <div class="narrow-bottom">The following {$$page$tags#} tags relate to the stories on this page</div>
                                <div class="row tags">
                                    {$$page$tags#3|
                                        <div class="sixth">
                                            {%1|
                                                <a href="{path}">{label}</a>
                                            }
                                        </div>
                                    }
                                </div>
                            </div>
                        }
                    </div>
                }
            }
            {{footer}}
        </div>
        {{js}}
    </body>
</html>